# Runtime dependencies for the Vercel API (api/index.py).
# Streamlit is intentionally excluded — it is local/demo only.
# Add anthropic or openai here only when enabling live providers on Vercel.

fastapi>=0.100
pydantic>=2.0
httpx>=0.27
beautifulsoup4>=4.12
pyyaml>=6.0
python-dotenv>=1.0
